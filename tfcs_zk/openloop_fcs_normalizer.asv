%function openloop_fcs_normalizer takes tau and g2 and normalize it to the
%first row of g2 (meaning the first row will have amplitude 1), and curves
%are aligned from t_tide to tau(end).

function [tau,ng2]=openloop_fcs_normalizer(tau,g2,t_tide)


